{% if include.id %}
  {% assign event_row = page.[include.id] %}
{% else %}
  {% assign event_row = page.event_row %}
{% endif %}

{% assign posts = site.posts | where: "categories", "Event" %}

{% for post in posts  limit:4 %}
  <div class="event__item">
      <div class="event__item-teaser">
        <a href="{{ post.link_url | absolute_url }}">
          {% if post.image_url %}
            <img src="{{ post.image_url | absolute_url }}">
          {% else %}
            <h2 class="event__item-title">{{ post.title }}</h2>
          {% endif %}
        </a>
      </div>
      <div class="event__item-body">
          <h4 class="date">
              <i class="fa fa-calendar-check-o" aria-hidden="true"></i> {{ post.event_date }}
          </h4>
          <h4 class="location">
              <i class="fa fa-map-marker" aria-hidden="true"></i> {{ post.event_location }}
          </h4>
          <div class="event__item-excerpt">
              {{ post.excerpt }}
          </div>
      </div>
  </div>

{% endfor %}
